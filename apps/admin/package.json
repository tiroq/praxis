{
  "name": "praxis-admin",
  "private": true,
  "version": "0.1.0",
  "description": "Scaffold for the Praxis admin interface.",
  "scripts": {
    "dev": "echo 'TODO: add admin frontend toolchain and dev server'",
    "build": "echo 'TODO: add admin frontend build configuration'",
    "test": "echo 'No admin tests configured yet'"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
